{
  "compilerOptions": {
    "types": ["vite/client", "vue"], // Certifique-se de que esses tipos estão incluídos
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.vue"] // Inclua os arquivos Vue corretamente
}
